{
  "name": "uniclaude-installer",
  "version": "0.1.0",
  "private": true,
  "description": "UniClaude install-mode conversion + git clean/smudge filter. Vanilla Node, no deps.",
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs"
  }
}
